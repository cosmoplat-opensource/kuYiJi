<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cosmo.hhim.micro.base.domain.mapper.common.MicroTenantIndividuationConfigMapper">

    <resultMap type="MicroTenantIndividuationConfig" id="MicroTenantIndividuationConfigResult">
        <result property="id" column="id"/>
        <result property="submitInspectSwitch" column="submit_inspect_switch"/>
        <result property="batchSubmitSwitch" column="batch_submit_switch"/>
        <result property="workerBaseDataConfine" column="worker_base_data_confine"/>
        <result property="createdBy" column="created_by"/>
        <result property="createdDate" column="created_date"/>
        <result property="lastUpdBy" column="last_upd_by"/>
        <result property="lastUpdDate" column="last_upd_date"/>
    </resultMap>

    <sql id="selectMicroTenantIndividuationConfigVo">
        select id, submit_inspect_switch, batch_submit_switch, worker_base_data_confine, created_by, created_date, last_upd_by, last_upd_date, tenant_code from micro_tenant_individuation_config
    </sql>
    
    <select id="selectMicroTenantIndividuationConfigByTenant" resultMap="MicroTenantIndividuationConfigResult">
        <include refid="selectMicroTenantIndividuationConfigVo"/>
        where tenant_code = #{tenantCode}
    </select>

    <update id="updateMicroTenantIndividuationConfig" parameterType="MicroTenantIndividuationConfig">
        update micro_tenant_individuation_config
        <trim prefix="SET" suffixOverrides=",">
            <if test="submitInspectSwitch != null and submitInspectSwitch != ''">submit_inspect_switch = #{submitInspectSwitch},</if>
            <if test="batchSubmitSwitch != null and batchSubmitSwitch != ''">batch_submit_switch = #{batchSubmitSwitch},</if>
            <if test="workerBaseDataConfine != null and workerBaseDataConfine != ''">worker_base_data_confine = #{workerBaseDataConfine},</if>
            <if test="createdBy != null and createdBy != ''">created_by = #{createdBy},</if>
            <if test="createdDate != null">created_date = #{createdDate},</if>
            <if test="lastUpdBy != null">last_upd_by = #{lastUpdBy},</if>
            <if test="lastUpdDate != null">last_upd_date = #{lastUpdDate},</if>
        </trim>
        where id = #{id}
    </update>

    <insert id="insertMicroTenantIndividuationConfig">
        insert into micro_tenant_individuation_config
        (tenant_code, submit_inspect_switch, batch_submit_switch, worker_base_data_confine, created_by, created_date, last_upd_by, last_upd_date)
        values (#{tenantCode}, #{microTenantIndividuationConfig.submitInspectSwitch}, #{microTenantIndividuationConfig.batchSubmitSwitch}, #{microTenantIndividuationConfig.workerBaseDataConfine}, #{microTenantIndividuationConfig.createdBy}, #{microTenantIndividuationConfig.createdDate}, #{microTenantIndividuationConfig.lastUpdBy}, #{microTenantIndividuationConfig.lastUpdDate})
    </insert>

    <!-- 幂等保存：tenant_code 唯一键冲突时自动转更新（MySQL 原子操作，并发安全）。
         依赖表唯一约束 uk_tenant_code(tenant_code)；update 分支不覆盖 created_by/created_date（保留首次创建信息）。 -->
    <insert id="upsertMicroTenantIndividuationConfig">
        insert into micro_tenant_individuation_config
        (tenant_code, submit_inspect_switch, batch_submit_switch, worker_base_data_confine, created_by, created_date, last_upd_by, last_upd_date)
        values (#{tenantCode}, #{microTenantIndividuationConfig.submitInspectSwitch}, #{microTenantIndividuationConfig.batchSubmitSwitch}, #{microTenantIndividuationConfig.workerBaseDataConfine}, #{microTenantIndividuationConfig.createdBy}, #{microTenantIndividuationConfig.createdDate}, #{microTenantIndividuationConfig.lastUpdBy}, #{microTenantIndividuationConfig.lastUpdDate})
        on duplicate key update
        submit_inspect_switch = values(submit_inspect_switch),
        batch_submit_switch = values(batch_submit_switch),
        worker_base_data_confine = values(worker_base_data_confine),
        last_upd_by = values(last_upd_by),
        last_upd_date = values(last_upd_date)
    </insert>

</mapper>