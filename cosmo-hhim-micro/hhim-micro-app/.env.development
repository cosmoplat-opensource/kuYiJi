# H5 开发环境配置
# API 走相对路径 /api，由 vite dev proxy（vite.config.ts）转发到 http://localhost:9010
# 好处：1) 手机浏览器访问时请求指向 dev server 所在机器（而不是手机自身的 localhost）
#       2) dev 阶段无跨域问题（代理在同域转发）
# 仅 H5 编译时由 common.ts 的 #ifdef H5 分支读取。
# 小程序/App 端请勿使用 VITE_API_BASE（此处 /api 相对路径微信 wx.request 不支持），
# 小程序/App 用独立变量 VITE_MP_API_BASE（见 src/utils/common.ts #ifndef H5 分支）。
VITE_API_BASE=/api

# 小程序/App 端后端地址：不配置时默认 http://localhost:9010（后端与开发者工具同机时可用）。
# 真机调试时后端地址需手机可达（如 http://<后端服务器IP>:9010，配合"真机调试"使用；
# 预览/体验/正式版受微信合法域名校验，必须 https 备案域名并配置 request 合法域名）。
# 示例：VITE_MP_API_BASE=http://192.168.1.100:9010

# 小程序/App 端指导视频地址（dev 构建，仅微信开发者工具使用）：
# 指向本机 vite dev server（vite.config.ts serveChineseStatic 插件支持中文文件名与 Range 分片），
# 避免开发者工具播放器加载内网 CDN 时出现 "Failed to load media / net::ERR_FAILED" 控制台报错。
# 注意：dev 构建产物仅用于开发者工具调试；真机调试/预览/发布请使用 build:mp-weixin（production，走 CDN）。
VITE_MP_VIDEO_BASE=http://localhost:8082/static/micro_app

