# H5 生产环境配置（build:h5 生效）
# API 走相对路径 /api，由部署环境的 nginx 反向代理到后端 9010（与 dev 的 vite proxy 对应，无跨域）
# 若后端已放开 CORS 且希望前端直连，可改为后端完整地址，例如：VITE_API_BASE=https://api.example.com
# 仅 H5 编译时由 common.ts 的 #ifdef H5 分支读取。
# 小程序/App 端请勿使用 VITE_API_BASE（此处 /api 相对路径微信 wx.request 不支持），
# 小程序/App 用独立变量 VITE_MP_API_BASE（见 src/utils/common.ts #ifndef H5 分支）。
VITE_API_BASE=/api

# 小程序/App 端后端地址：不配置时默认 http://localhost:9010（后端与开发者工具同机时可用）。
# 真机调试时后端地址需手机可达（如 http://<后端服务器IP>:9010，配合"真机调试"使用；
# 预览/体验/正式版受微信合法域名校验，必须 https 备案域名并配置 request 合法域名）。
# 示例：VITE_MP_API_BASE=http://192.168.1.100:9010

# 小程序/App 端静态资源地址（视频 + 图片统一从这里加载）：
# 指向你部署的 H5 静态目录（H5 构建产物已包含全部视频与图片，部署 H5 后直接可用）。
# 必须与微信公众平台配置的 downloadFile 合法域名一致（https + ICP 备案域名）。
# 未配置时小程序端回退包内路径（仅部分资源可用，真机不保证）。
VITE_MP_VIDEO_BASE=https://your-domain.com/static/micro_app
